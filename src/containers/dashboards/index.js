import React from 'react';

import { StyledHeader, StyledTitle } from 'styles/app';

export default () => (
  <StyledHeader>
    <StyledTitle>
      Dashboards
    </StyledTitle>
  </StyledHeader>
);
